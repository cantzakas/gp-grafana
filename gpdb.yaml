apiVersion: 1

datasources:
  - name: Postgres
    type: postgres
    url: localhost:5432
    isDefault: true
    database: gpperfmon
    user: gpmon
    secureJsonData:
      password: password
    jsonData:
      sslmode: "disable"
      postgresVersion: "8.3"
    version: 1
    editable: true
