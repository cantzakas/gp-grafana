apiVersion: 1

datasources:
- name: Postgres
  type: postgres
  url: localhost:5432
  isDefault: true
  database: gpperfmon
  user: gpmon
  secureJsonData:
    password: password
  jsonData:
    sslmode: "disable"
  version: 1
  editable: true
- name: Postgres-admin
  type: postgres
  url: localhost:5432
  isDefault: true
  database: gpadmin
  user: gpadmin
  secureJsonData:
    password: password
  jsonData:
    sslmode: "disable"
  version: 1
  editable: true